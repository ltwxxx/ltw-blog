version: '3.8'

services:
  homepage:
    build: .
    ports:
      - "8081:83"
    restart: unless-stopped
    environment:
      - NGINX_PORT=83
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:83/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
